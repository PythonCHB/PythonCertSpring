#! /usr/bin/python
#
# This doesn't work when run as a script, but works fine in the interpreter.
def f(x) :
    y = x**2
    return y

a = float(raw_input("Enter a floating point number"))
f(a)


          
